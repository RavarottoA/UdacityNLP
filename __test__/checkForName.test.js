import { checkForName } from "../src/client/js/formHandler"

describe("Testing the checking functionality", () => {
    test("Testing the checkForName() function", () => {
    function checkForName(data) {
        try {
            expect(checkForName).toBeDefined();
          done();
        } catch (error) {
          done(error);
        }
      }
})})